{
    "apiBaseUrl": "/api",
    "auth0" : {
        "domain": "${AUTH0_DOMAIN}",
        "clientId": "${AUTH0_CLIENT_ID}",
        "audience": "${AUTH0_AUDIENCE}"
    },
    "rollbarAccessToken": "${ROLLBAR_ACCESS_TOKEN}"
}
