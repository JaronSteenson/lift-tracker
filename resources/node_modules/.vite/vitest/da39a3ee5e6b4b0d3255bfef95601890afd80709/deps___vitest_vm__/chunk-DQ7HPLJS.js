import {
  VITAL_NAMES,
  VitalMetric,
  ft
} from "./chunk-23PX3NC3.js";
import {
  globalScope,
  isBrowserScope,
  isiOS,
  originTime
} from "./chunk-MTDZUOW2.js";

// node_modules/@newrelic/browser-agent/dist/esm/common/vitals/time-to-first-byte.js
var timeToFirstByte = new VitalMetric(VITAL_NAMES.TIME_TO_FIRST_BYTE);
if (isBrowserScope && typeof PerformanceNavigationTiming !== "undefined" && !isiOS && window === window.parent) {
  ft(({
    value,
    attribution
  }) => {
    if (timeToFirstByte.isValid) return;
    timeToFirstByte.update({
      value,
      attrs: {
        navigationEntry: attribution.navigationEntry
      }
    });
  });
} else {
  if (!timeToFirstByte.isValid) {
    const entry = {};
    for (let key in globalScope?.performance?.timing || {}) entry[key] = Math.max(globalScope?.performance?.timing[key] - originTime, 0);
    timeToFirstByte.update({
      value: entry.responseStart,
      attrs: {
        navigationEntry: entry
      }
    });
  }
}

export {
  timeToFirstByte
};
//# sourceMappingURL=chunk-DQ7HPLJS.js.map
