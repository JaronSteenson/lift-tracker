import {
  LOGGING_EVENT_EMITTER_CHANNEL,
  LOG_LEVELS
} from "./chunk-K7YPRITW.js";
import {
  FEATURE_NAMES,
  SUPPORTABILITY_METRIC_CHANNEL,
  handle,
  now
} from "./chunk-MTDZUOW2.js";

// node_modules/@newrelic/browser-agent/dist/esm/features/logging/shared/utils.js
function bufferLog(ee, message, customAttributes = {}, level = LOG_LEVELS.INFO, targetEntityGuid, timestamp = now()) {
  handle(SUPPORTABILITY_METRIC_CHANNEL, ["API/logging/".concat(level.toLowerCase(), "/called")], void 0, FEATURE_NAMES.metrics, ee);
  handle(LOGGING_EVENT_EMITTER_CHANNEL, [timestamp, message, customAttributes, level, targetEntityGuid], void 0, FEATURE_NAMES.logging, ee);
}
function isValidLogLevel(level) {
  if (typeof level !== "string") return false;
  return Object.values(LOG_LEVELS).some((logLevel) => logLevel === level.toUpperCase().trim());
}

export {
  bufferLog,
  isValidLogLevel
};
//# sourceMappingURL=chunk-QCLFB3RN.js.map
